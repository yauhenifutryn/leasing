# Python
__pycache__/
*.py[cod]
*.pyo

# Claude Code local config (permissions, hooks, etc). Local-only — never commit.
.claude/

# Virtual environments
.venv/
.venv*/
.venv_dedup/
env/
venv/

# OS / tooling noise
.DS_Store
.idea/
.vscode/
.playwright-cli/
tmp/
output/
node_modules/

# Secrets
.env
AGENTS.md
CONTINUITY.md
PROJECT_LOG.md
UI_TRINITY.md
docs/plans/2026-01-20-project-log-design.md

# External-facing handover docs (local artifacts, send via email/Slack — never commit).
ARCHITECTURE_OVERVIEW_RU.md
ANALYSIS.md
ONBOARDING.md
HANDOVER.md

# Generated audio + transcripts + outputs
audio/
scratch_audio/
transcripts_raw/
transcripts_clean/
insights_per_call/
insights_batches/
insights_global/
nlu_output/
corrections/

# Accidental artifact folders (should never be committed)
\#/
doesn’t/
exist/
if/
only/
yet/

# Demo UI runtime state (sessions, logs, feedback)
demo_ui/.state/
demo_ui/__pycache__/

# Archives
*.zip
*.tar.gz

# Voice logs dump (analysis artifacts)
voice-logs/
voice-logs.tar.gz

# Voice pipeline runtime artifacts
**/voices/*.pt
**/voices/*.wav
**/voices/catalog.json
!**/voices/.gitkeep
models/
*.log
.state/

.worktrees/

# Internal developer-only docs (local workspace, never commit).
# Ignore everything under docs/ by default; allow only explicit client-facing
# deliverables through with `!` exceptions below.
docs/**
!docs/calculator-api-production-spec-ru.md
