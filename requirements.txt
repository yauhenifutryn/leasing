# PyTorch is installed via Makefile to ensure Nightly version for RTX 5090
# torch, torchvision, torchaudio removed from here
# WhisperX installed separately to avoid dependency conflicts (see Makefile)
# whisperx @ git+https://github.com/m-bain/whisperx.git
faster-whisper==1.2.1
pyannote.audio==3.4.0
protobuf==4.25.3
pandas==2.2.3
orjson
python-dotenv
tqdm
huggingface-hub==0.24.5
transformers==4.37.2
tokenizers==0.15.2
# Other dependencies from original requirements that might still be needed
openai>=1.50.0
pydantic>=2.8.2
sentence-transformers==2.2.2
numpy>=2.0.0
scikit-learn>=1.5.1
tabulate>=0.9.0
langcodes>=3.4.0
streamlit>=1.37.0
matplotlib>=3.9.0
