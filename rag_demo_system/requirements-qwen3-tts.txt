qwen-tts==0.1.1
# torch and torchaudio are NOT listed here. They require --index-url
# https://download.pytorch.org/whl/cu126 (cu124 was dropped in torch 2.7).
# provision_server.sh installs them separately before this file.
# doctor.sh verifies and fixes the versions if needed.
fastapi==0.115.6
uvicorn==0.30.6
pydantic>=2.10.0
soundfile
