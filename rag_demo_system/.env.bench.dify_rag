# Benchmark profile: dify_rag
# Dify RAG backend instead of our_rag
# All other settings inherit from baseline via runner's overlay mechanism
# Requires DIFY_API_BASE_URL and DIFY_API_KEY to be set in base .env
BENCH_PROFILE=dify_rag
BENCH_BACKEND=dify_rag
