fastapi==0.115.6
uvicorn==0.30.6
pydantic==2.10.2
# CTranslate2 (used by faster-whisper) must match the NVIDIA driver's PTX support.
# CTranslate2 >= 4.5.0 is compiled with CUDA 12.8 (PTX ISA 8.7), which requires
# driver >= 570. Jarvis Labs and many bare metal servers ship driver 550 (PTX ISA 8.4).
# Pin CTranslate2 < 4.5.0 to avoid PTX version mismatch on driver 550.
ctranslate2>=4.0.0,<4.5.0
faster-whisper>=1.0.0,<1.1.0
nvidia-cublas-cu12
nvidia-cudnn-cu12
silero>=0.5.0
scipy
