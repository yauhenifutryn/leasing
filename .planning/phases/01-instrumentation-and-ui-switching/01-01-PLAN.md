---
phase: 01-instrumentation-and-ui-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rag_demo_system/backend/voice_session.py
  - rag_demo_system/tests/test_voice_session.py
  - rag_demo_system/tests/test_instrumentation.py
autonomous: true
requirements: [INST-01, SWITCH-03]

must_haves:
  truths:
    - "VoiceSession has brain_model, stt_provider, tts_provider fields with correct defaults"
    - "stack_id is a computed property combining backend, brain_model, stt_provider, tts_provider"
    - "stack_id changes when any underlying field changes"
    - "All new fields and stack_id have passing unit tests"
  artifacts:
    - path: "rag_demo_system/backend/voice_session.py"
      provides: "Extended VoiceSession dataclass with 3 new fields + stack_id property"
      contains: "brain_model"
    - path: "rag_demo_system/tests/test_voice_session.py"
      provides: "Unit tests for new fields and stack_id"
      contains: "test_stack_id_composition"
    - path: "rag_demo_system/tests/test_instrumentation.py"
      provides: "Test scaffold for log event structure (used by Plan 02)"
      contains: "test_voice_turn_log_event"
  key_links:
    - from: "rag_demo_system/tests/test_voice_session.py"
      to: "rag_demo_system/backend/voice_session.py"
      via: "import and instantiation"
      pattern: "VoiceSession\\("
---

<objective>
Extend VoiceSession dataclass with brain_model, stt_provider, tts_provider fields and a computed stack_id property. Create the test scaffolding for this phase (Wave 0 gaps from research).

Purpose: All subsequent plans depend on VoiceSession having these fields. Tests must exist before implementation proceeds.
Output: Updated voice_session.py, extended test_voice_session.py, new test_instrumentation.py scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-instrumentation-and-ui-switching/01-RESEARCH.md

<interfaces>
<!-- Current VoiceSession: executor must see full current state before modifying -->

From rag_demo_system/backend/voice_session.py:
```python
@dataclass
class VoiceSession:
    session_id: str
    backend: str = "our_rag"
    voice_provider: str = "local"
    assistant_speaking: bool = False
    interrupted: bool = False
    active_task_id: str | None = None
    last_user_message: str = ""
```

From rag_demo_system/backend/state.py (StateStore.log signature):
```python
def log(self, event: dict[str, Any]) -> None:
    with self.logs_path.open("a", encoding="utf-8") as handle:
        handle.write(json.dumps(event, ensure_ascii=False))
        handle.write("\n")
```

From rag_demo_system/tests/test_voice_session.py (existing test pattern):
```python
def _load_module():
    import importlib, importlib.util
    spec = importlib.util.find_spec("backend.voice_session")
    assert spec is not None, "backend.voice_session module is missing"
    return importlib.import_module("backend.voice_session")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend VoiceSession with new fields and stack_id property</name>
  <files>rag_demo_system/backend/voice_session.py, rag_demo_system/tests/test_voice_session.py</files>
  <read_first>
    rag_demo_system/backend/voice_session.py
    rag_demo_system/tests/test_voice_session.py
  </read_first>
  <behavior>
    - test_default_brain_model: VoiceSession(session_id="s1").brain_model == "Qwen/Qwen3-30B-A3B"
    - test_default_stt_provider: VoiceSession(session_id="s1").stt_provider == "sensevoice"
    - test_default_tts_provider: VoiceSession(session_id="s1").tts_provider == "cosyvoice"
    - test_stack_id_composition: VoiceSession(session_id="s1", backend="our_rag", brain_model="Qwen/Qwen3-30B-A3B", stt_provider="sensevoice", tts_provider="cosyvoice").stack_id == "our_rag__Qwen3-30B-A3B__sensevoice__cosyvoice"
    - test_stack_id_strips_prefix: VoiceSession(session_id="s1", brain_model="Qwen/Qwen3.5-35B-A3B").stack_id contains "Qwen3.5-35B-A3B" not "Qwen/"
    - test_stack_id_updates_on_field_change: session = VoiceSession(session_id="s1"); session.backend = "dify_rag"; session.stack_id starts with "dify_rag__"
    - test_existing_tests_still_pass: all 4 existing tests remain green
  </behavior>
  <action>
    1. Add 5 new test functions to rag_demo_system/tests/test_voice_session.py following the existing _load_module() pattern:
       - test_default_brain_model, test_default_stt_provider, test_default_tts_provider
       - test_stack_id_composition (exact string match)
       - test_stack_id_updates_on_field_change

    2. Run tests, confirm they fail (RED).

    3. Edit rag_demo_system/backend/voice_session.py:
       - Add three new fields to VoiceSession dataclass AFTER voice_provider and BEFORE assistant_speaking:
         brain_model: str = "Qwen/Qwen3-30B-A3B"
         stt_provider: str = "sensevoice"
         tts_provider: str = "cosyvoice"
       - Add a @property for stack_id:
         @property
         def stack_id(self) -> str:
             brain = self.brain_model.split("/")[-1]
             return f"{self.backend}__{brain}__{self.stt_provider}__{self.tts_provider}"

    4. Run tests, confirm all pass (GREEN).

    ANTI-PATTERNS (from research):
    - Do NOT store stack_id as a mutable field. It MUST be a @property.
    - Do NOT remove the voice_provider field. It is still used for yandex_realtime relay detection.
  </action>
  <verify>
    <automated>cd /Users/jenyafutrin/workspace/coding_projects/leasing && python3 -m pytest rag_demo_system/tests/test_voice_session.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - voice_session.py contains `brain_model: str = "Qwen/Qwen3-30B-A3B"`
    - voice_session.py contains `stt_provider: str = "sensevoice"`
    - voice_session.py contains `tts_provider: str = "cosyvoice"`
    - voice_session.py contains `@property` followed by `def stack_id(self)`
    - voice_session.py contains `self.brain_model.split("/")[-1]`
    - test_voice_session.py contains `test_stack_id_composition`
    - test_voice_session.py contains `test_default_brain_model`
    - pytest test_voice_session.py exits 0 with 9 passed (4 existing + 5 new)
  </acceptance_criteria>
  <done>VoiceSession has brain_model, stt_provider, tts_provider fields with correct defaults. stack_id property returns "backend__brain__stt__tts" format. All 9 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_instrumentation.py scaffold for log event verification</name>
  <files>rag_demo_system/tests/test_instrumentation.py</files>
  <read_first>
    rag_demo_system/tests/test_voice_session.py
    rag_demo_system/backend/state.py
  </read_first>
  <action>
    Create rag_demo_system/tests/test_instrumentation.py with the following test functions. These tests verify the log event STRUCTURE, not the WebSocket handler. They test a helper function that Plan 02 will implement.

    The test file must:
    1. Use the same _load_module() pattern from test_voice_session.py but import "backend.voice_session"
    2. Define REQUIRED_LOG_FIELDS as a set:
       {"event", "question_id", "stack_id", "session_id", "backend", "brain_model",
        "stt_provider", "tts_provider", "transcript",
        "speech_stopped", "stt_done", "retrieval_done", "llm_first_token",
        "tts_first_chunk", "playback_started", "primary_kpi_ms"}

    3. Implement test functions:

       test_voice_turn_log_event_has_all_required_fields():
         - Build a sample event dict with all 17 fields from REQUIRED_LOG_FIELDS
         - Assert REQUIRED_LOG_FIELDS.issubset(event.keys())

       test_primary_kpi_ms_is_computed_correctly():
         - Given speech_stopped=1000.0, playback_started=1002.5
         - Assert primary_kpi_ms == 2500.0

       test_stack_id_matches_session_fields():
         - Create a VoiceSession(session_id="test", backend="our_rag")
         - Assert session.stack_id == "our_rag__Qwen3-30B-A3B__sensevoice__cosyvoice"
         - This test bridges VoiceSession and the log event contract

    All tests must pass immediately (these validate structure, not runtime behavior).
  </action>
  <verify>
    <automated>cd /Users/jenyafutrin/workspace/coding_projects/leasing && python3 -m pytest rag_demo_system/tests/test_instrumentation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - rag_demo_system/tests/test_instrumentation.py exists
    - test_instrumentation.py contains `REQUIRED_LOG_FIELDS`
    - test_instrumentation.py contains `test_voice_turn_log_event_has_all_required_fields`
    - test_instrumentation.py contains `test_primary_kpi_ms_is_computed_correctly`
    - test_instrumentation.py contains `test_stack_id_matches_session_fields`
    - test_instrumentation.py contains `"primary_kpi_ms"`
    - pytest test_instrumentation.py exits 0 with 3 passed
  </acceptance_criteria>
  <done>Test scaffold exists with 3 passing tests that define the log event contract for Plan 02.</done>
</task>

</tasks>

<verification>
python3 -m pytest rag_demo_system/tests/test_voice_session.py rag_demo_system/tests/test_instrumentation.py -x -q
# Expected: 12 passed (9 voice_session + 3 instrumentation)
</verification>

<success_criteria>
- VoiceSession has brain_model, stt_provider, tts_provider fields
- stack_id property returns correct composite string
- Test files exist and all 12 tests pass
- Existing 4 voice_session tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrumentation-and-ui-switching/01-01-SUMMARY.md`
</output>
